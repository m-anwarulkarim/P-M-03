/*
📦 Payload মানে কী?

Express (বা যেকোনো API তে) Payload বলতে বোঝায়:
👉 ক্লায়েন্ট (ফ্রন্টএন্ড / পোস্টম্যান) যে ডেটা সার্ভারে পাঠায়।

এই ডেটা সাধারণত আসে:

req.body

req.params

req.query

(কখনো কখনো header-এও data থাকে)

🧩 Payload = Request Body (সবচেয়ে বেশি ব্যবহৃত)

উদাহরণ: তুমি যদি Postman থেকে পাঠাও —

{
  "name": "Anwar",
  "email": "test@example.com",
  "age": 20
}


এই JSON পুরোটা হলো payload।

Express এ তুমি এটা ধরবে:

app.post("/user", (req, res) => {
  const payload = req.body;

  console.log(payload);
  // { name: 'Anwar', email: 'test@example.com', age: 20 }

  res.json({ received: payload });
});



======================================
🔹 bcryptjs কি?

bcryptjs হলো একটি JavaScript library

মূল কাজ: password hash করা এবং verify করা

Node.js বা frontend JS প্রোজেক্টে ব্যবহার করা যায়

নিরাপদভাবে পাসওয়ার্ড স্টোর করার জন্য ব্যবহৃত হয়

🔹 bcryptjs এর প্রধান কাজ

Password Hashing (এনক্রিপ্ট করা)

User যখন password দেয় → সরাসরি DB তে রাখার বদলে hash করে রাখা হয়

উদাহরণ:

import bcrypt from "bcryptjs";

const password = "123456";
const hashed = await bcrypt.hash(password, 10); 
console.log(hashed);


Output হবে কিছু এভাবে:

$2a$10$3bE2N7GmG5Jc5YwT1sUQeOeQ... 


Password Verification (মিলিয়ে দেখা)

Login করার সময় user যে password দেয়, hash এর সাথে মিলিয়ে দেখা হয়

উদাহরণ:

const isMatch = await bcrypt.compare("123456", hashed);
console.log(isMatch); // true বা false

🔹 কেন bcryptjs ব্যবহার করা হয়?

Password সরাসরি DB তে রাখা অত্যন্ত অসুরক্ষিত

Hashing → password কে safe করে দেয়

Hackers DB access পেলেও আসল password পাবে না

Salt + hashing → password guessing/attacks থেকে সুরক্ষা










        --------------------------------
        |                               |
        |                               |
        |                               |
        |                               |
        |                               |
        |                               |
        |                               |
        |                               |
        |                               |
        |                               |
        |                               |
        |                               |
        |                               |
        |                               |
        |                               |

*/
