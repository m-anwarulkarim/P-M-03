/* 
========================================================
১. Express কী?
--------------------------------------------------------
Express হলো Node.js এর উপর তৈরি একটি web framework.
এটা দিয়ে খুব সহজে server তৈরি করা যায়, API বানানো যায়,
routing করা যায়, middleware use করা যায় ইত্যাদি।

সংক্ষেপে: Express = Node.js server বানানোকে অনেক সহজ করে।
========================================================
*/

/* 
========================================================
২. Installation Steps (ধাপে ধাপে)

A. npm init -y
   → প্রজেক্ট initialize করবে এবং package.json তৈরি করবে।

B. npm install express --save
   → Express dependency ইনস্টল করবে।

C. npm i -D typescript
   → TypeScript কে dev dependency হিসেবে ইনস্টল করবে।

D. npx tsc --init
   → tsconfig.json ফাইল তৈরি করবে।

E. tsconfig.json সেটআপ:
   → "rootDir" এবং "outDir" লাইনগুলো Comment করে দেবে।
   → "moduleResolution", "target", "verbatimModuleSyntax", "jsx" 
      এগুলোও কমেন্ট করে দেবে যেন default config এ চলে।

F. Express এর types install:
   npm i -D @types/express

G. npm i -D tsx
   → tsx দিয়ে TS ফাইল run/watcher চালানো হবে।

H. package.json scripts:
   "dev": "npx tsx watch example.ts"
========================================================
*/

/*
========================================================
এখন Express ব্যবহার করে একটি ছোট Server তৈরি করা হলো।
========================================================
*/

// Express import
import express, { Request, Response } from "express";

// App তৈরি
const app = express();

// Port
const PORT = 3000;

/*
========================================================
এখানে Home route তৈরি করা হলো
--------------------------------------------------------
User যখন "/" রুটে যাবে তখন "Server is running" দেখবে
========================================================
*/
app.get("/", (req: Request, res: Response) => {
  res.send("Server is running with Express + TypeScript 🚀");
});

/*
========================================================
সার্ভার চালু করার লাইন
========================================================
*/
app.listen(PORT, () => {
  console.log(`Server started on port ${PORT}`);
});
